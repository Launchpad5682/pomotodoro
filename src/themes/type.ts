export type ThemeType = {
  backgroundColor: string;
  darkColor: string;
  textColor: string;
  shadowColorOne: string;
  shadowColorTwo: string;
  boxShadow: string;
  buttonShadow: string;
  buttonHoverShadow: string;
  iconSize: number;
  iconShadow: string;
  iconShadowHover: string;
  iconBackgroundColor: string;
  inputFieldShadow: string;
  checkboxShadow: string;
  checkboxShadowChecked: string;
  checkboxBackgroundColor: string;
};
